# 录屏参考效果：文字揭示与工业材质

分析依据：用户提供的约 19.83 秒本地录屏。没有读取原站源代码，以下实现为视觉近似，不代表原站技术实现。

## 观察

- 约 2.8–4.3 秒：小黑块横向铺开成遮挡条，随后擦除露出标题；下方文字错开出现。不是逐字打字机，也不是随机乱码切换。
- 约 10–12 秒：黑色面板上沿有亮边，中部较哑光，下沿深色；橙色外壳配切角、机械铆钉和阴影。
- 车辆反光来自画面中的渲染素材；仅凭视频不能确认网页是否实时运行 Three.js。

## 工作台入口

1. 选中文字 → 属性 → **文字出现预设**。选择“色块铺开 → 擦除露字”或“逐行切入”。
2. 调整行间错开、遮罩色、方向，以及下方的时长/延迟。默认 0.85 秒；手动换行分组，自动折行保持同组。
3. 点击“预览此页动效”，或进入预览后点击“重播”。编辑模式不隐藏文字；减少动态效果时直接显示文字。
4. 选中章节或文字 → **工业材质**。选择喷漆外壳、黑色软胶或拉丝金属，再调整底色、高光、颗粒、包边、铆钉与切角。图片只叠加边缘/颗粒/铆钉，不覆盖原图；切角只改变底板轮廓。
5. 选中 3D 图层 → **3D 材质快捷预设**。喷漆/橡胶/金属仅修改已有材质参数，保留模型几何。需要 WebGL，静态替代图不会实时改变。

## 范围与安全

- 原始录屏、截帧和解码工具仅用于本地分析，位于忽略目录，不加入网站或提交。
- 不复制参考网站的字体、标志、车辆、贴纸或人物素材。
- 预设是可选项，不自动覆盖现有设计。新字段可选，旧 V2 草稿可继续导入；新效果随本机草稿和 JSON 备份保存。
- 材质为 CSS 视觉近似，不是 PBR 贴图。3D 快捷项使用已有物理材质参数，不包含参考模型。
- 本次只扩展本地 `/studio-next`，未发布线上，未修改 v1 Worker。

## 验证

- 18 项自动测试通过，覆盖旧文档兼容、新预设往返、恶意输入拒绝及错开时长上限。
- 浏览器确认预设选择、材质显示、文字最终可见与撤销；试验修改已撤销，保留用户原有工作状态。
- 生产构建通过。精确动效帧时序和 WebGL 材质的跨设备表现仍需设备实测。
