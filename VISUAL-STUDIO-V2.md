# Visual Studio V2 — 第一阶段工作副本

本地入口：`http://127.0.0.1:3001/studio-next/`。运行方式与原项目相同：`npm run dev`。

开发分支：`studio-visual-v2`。未推送、未部署。原首页、详情页、`/studio/` 和 Cloudflare Worker 均未修改。

## 已实现

- 五个页面的可编辑起始文档，独立于 v1 配置；使用原有概念项目文字与图片。
- 图层选择、拖动、8px 吸附（Alt 关闭）、缩放手柄、方向键微调、复制、删除、前后排序、锁定、隐藏。
- 画布直接输入文字；尺寸、位置、字距、行高、颜色、透明度、旋转、圆角与对齐。
- 章节自由布局或简单横向/纵向自动布局；内边距、间距、背景与渐变。
- 桌面、平板、手机覆盖样式；手机提供独立堆叠排版。小屏工作台在图层、画布、属性之间切换。
- 图片上传、压缩、替换、裁切焦点、替代文字和当前文档素材复用。上传保留框架尺寸。
- SVG 几何白名单、复杂度限制、闭合路径检查；挤出/倒角/材质参数。模型默认沿用程序生成雕塑，可导入 SVG 替换。
- 进入淡入、位移、延迟、时长和悬停缩放；预览重播，遵循减少动态效果设置。
- 30 步撤销/重做（拖动一次计一步）；IndexedDB 显式本机保存/恢复，含图片和 SVG 的 JSON 导出/导入。
- 本机存储失败、格式错误、素材限制有明确错误提示。不会将上传内容自动发送到网络。

## 明确未完成

- 当前迁移是可编辑的起始版，不是原网站像素级还原；原站 Experimental、导航、项目链接、Footer 时钟等尚未接入新版渲染器。
- 目前只有章节容器，没有任意嵌套、多选、组合、完整 Auto Layout 的 hug/fill、复杂对齐辅助线或时间轴。
- 尚未接入新版 GitHub 登录、加密云端草稿、原子多文件发布、冲突处理与发布状态。原 v1 后台不能接收 v2 文档，禁止将本机备份直接发给 v1 发布接口。
- 未切换正式网站渲染器；预览与编辑共用新渲染器，正式网站仍用原渲染器。
- 不支持 GLB、顶点编辑、布尔运算。SVG 的文字和描边需先转轮廓；不支持外链、样式表、滤镜、蒙版或图片。
- 手机/无 WebGL 时显示用户指定的静态替代图，**不会自动生成新 SVG 模型的截图**；更换模型后应同时更新替代图。
- 尚未加入滚动动画、链接动作、分材质、灯光编辑与历史发布恢复。

## 验证记录

- 生产静态构建通过；13 项自动测试通过，包括原 v1 安全测试和 v2 迁移、断点隔离、图片替换保留尺寸及恶意输入测试。
- 浏览器检查了文字 X 坐标修改/撤销、本机保存、手机预览、SVG 选择导入。检查了 1920×1080 与 390×844 视口。
- 测试浏览器显示静态替代图，**实时 WebGL 挤出结果、材质视觉质量和帧率尚未验收**。
- 新代码 ESLint 无错误；保留三处原生 img 性能提示。真实图片上传/刷新恢复/拖动手势完整回归仍需补测。

## 下一阶段

先完善渲染器与文档迁移、模型 WebGL 实测，再升级受限 Worker 接口和 GitHub 原子提交；通过跨设备登录、草稿冲突、发布与回滚验证后，才替换正式编辑器及网站。保留 v1 文件与旧渲染器作为回退路径。
